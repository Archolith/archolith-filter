#!/usr/bin/env sh
set -eu

while read -r local_ref local_sha remote_ref remote_sha
do
    if [ "$remote_ref" = "refs/heads/main" ]; then
        printf '%s\n' "Push to main is blocked. Use a feature branch." >&2
        exit 1
    fi
done

exit 0
